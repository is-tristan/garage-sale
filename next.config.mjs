/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  turbopack: {
    root: '/home/tristan/next/garage-sale',
  },
};

export default nextConfig;
